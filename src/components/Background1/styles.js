import styled from 'styled-components'
import { colors } from '../../styles'

export const BlueContainer = styled.View.attrs(props => props.colors.primary)`
`;

export const WhiteContainer = styled.View`

`;